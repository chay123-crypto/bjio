name: Validate and Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: '24f2002314@ds.study.iitm.ac.in'
        run: echo "Hello, world!"
